import { createTheme } from "@material-ui/core/styles";

const themeMui = createTheme({
  palette: {
    primary: {
      light: "#6ec6ff",
      main: "#2196f3",
      dark: "#0069c0",
      contrastText: "#ffffff",
    },
  },
});

export default themeMui;
